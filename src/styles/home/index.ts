import {backgroundColor, textColor} from '../global';
import {StyleSheet} from 'react-native';

export const homeStyle = StyleSheet.create({
  home: {
    backgroundColor,
    textColor,
    display: 'flex',
    flex: 1,
  },
});
